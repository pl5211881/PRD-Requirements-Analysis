# PRD Sentinel Design Guidelines

这份文档用于约束后续 UI 修改。任何 AI 或开发者在调整界面时，应优先遵守这里的设计规则，再考虑局部优化。

## 1. 视觉风格定位

本项目是面向产品、设计、研发协作的 PRD 可视化分析工作台。整体气质应保持“专业、冷静、技术感、可审查”，而不是营销落地页或普通后台。

核心视觉关键词：

- 暗黑工作台：以深黑和低亮度面板承载高密度信息。
- 荧光绿识别：用 `#b7ff5a` 作为唯一强主色，用于关键行动、选中态、状态聚焦和品牌识别。
- 毛玻璃与网格：使用半透明玻璃卡片、背景模糊、细网格线和 shader 光效营造深度。
- 信息优先：界面服务于阅读、对比、审查和定位，不做大面积装饰性插画。
- 设计决策导向：分析结果优先突出“设计待办、设计依据、PRD 问题清单”，不要回退成普通 PRD 文档堆叠。

## 2. 色彩规范

### 基础色

- 页面背景：`#090b0f`
- 背景过渡：`#0b0f14`
- 主面板：`#11151c`
- 次级面板：`#0d1117`
- 主文字：`#f5f7fb`
- 弱文字：`#808080`
- 分割线：`rgba(255, 255, 255, 0.08)`
- 强分割线：`rgba(255, 255, 255, 0.16)`

### 主色

- 荧光绿：`#b7ff5a`
- 主色弱背景：`rgba(183, 255, 90, 0.14)`
- 主色边框：`rgba(183, 255, 90, 0.28)` 到 `rgba(183, 255, 90, 0.42)`

荧光绿只用于：

- 当前选中 Tab
- 主按钮
- 品牌文字
- 上传区域边框和文件状态
- 设计重点、优先级标签、定位符
- 背景 shader / 网格的低透明度光效

不要把荧光绿用于大面积背景。高亮区域应保持低透明度，避免抢走阅读焦点。

### 语义色

- 致命：`red-500/12` 背景，`red-200` 文字，`red-400/22` 描边
- 严重：`amber-400/12` 背景，`amber-100` 文字，`amber-300/24` 描边
- 轻微：`white/[0.07]` 背景，`white/68` 文字，`white/12` 描边
- 优秀：`emerald-400/12` 背景，`emerald-100` 文字，`emerald-300/22` 描边

语义色必须低饱和、低面积，适配暗黑风格；不要使用纯红、纯黄、纯绿的大块警示色。

## 3. 排版规范

### 字体

- 全局字体基调使用 `font-mono`，保持工具感和技术感。
- 标题使用 `font-heading`。
- 不要擅自引入新的字体依赖，除非要整体重做视觉体系。

### 字号层级

- 首页主标题：`clamp(2.5rem, 5.4vw, 4.8rem)`，行高约 `1.12`
- 页面标题：`text-xl`，`font-semibold`
- 卡片标题：`text-base` 到 `text-lg`，`font-semibold`
- 正文：`text-sm` 或 `text-base`，行高 `leading-6` 到 `leading-7`
- 标签：`text-xs`，高度紧凑，不做大块说明文字

### 文字规则

- 字间距保持 `letter-spacing: 0`，不要使用负字距。
- 暗色背景上的正文透明度通常在 `white/64` 到 `white/72`。
- 辅助说明透明度通常在 `white/42` 到 `white/58`。
- 页面内不应出现大段功能说明、使用教程或营销话术。

### 间距节奏

右侧分析结果遵循以下节奏：

- 分析区列表外层：`gap-4`
- 主卡片内距：`p-5`
- 软玻璃小卡片内距：`p-5`
- 标题到正文：`mt-3`
- 同级内容块之间：`mt-4`
- 子标题到子正文：`mt-2`
- 列表项之间：`space-y-2`
- 证据区顶部：`border-t` + `pt-4`

不要在同一父子关系里混用 `mt-3` 和 `mt-4`。如果是标题到正文，用 `mt-3`；如果是内容块到内容块，用 `mt-4`。

## 4. 组件规范

### 页面结构

应用分为两种状态：

- 上传态：首页式工作台，左侧说明和指标，右侧上传与配置面板。
- 分析态：顶部文件与操作栏，左侧文档预览，右侧分析结果，底部定位符。

分析态布局规则：

- 左侧文档预览区占比应小于右侧分析区。
- 左侧显示完整 PDF viewer 或 Markdown/TXT 原文阅读，不展示 PDF 缩略图。
- 不提供文档预览收起/展开入口。
- 右侧是主要工作区，必须可独立滚动。

### 卡片

主分析卡片使用 `.helix-card`：

- 背景：深色面板 + 低透明荧光绿径向光。
- 边框：`var(--helix-line)`
- 圆角：`24px`
- 内距：`p-5`
- hover：轻微上移 `translateY(-3px)`，边框变为荧光绿弱描边。

毛玻璃卡片使用 `.helix-panel` 或 `.helix-panel-soft`：

- 使用半透明深色背景。
- 必须开启 `backdrop-filter`。
- 适合上传面板、指标卡、弹层和轻量信息块。

不要把页面大区块全部做成浮动卡片。卡片只用于具体内容单元、输入面板、弹层和分析项。

### Tab

分析结果固定保留 3 个 Tab：

- `设计待办`：展示需要设计师产出的流程、功能、页面、状态。
- `设计依据`：展示业务目标、设计目标、用户场景、体验机会和需求输入。
- `PRD 问题清单`：展示给产品经理或业务方补材料的问题。

Tab 规则：

- Tab 容器使用 `.helix-tabs-strong`。
- Tab 宽度按内容自适应，不三等分铺满。
- 未选中态：暗色半透明背景，白色文字。
- 选中态：荧光绿背景，深色文字。
- 切换时不能出现白色闪底。
- Tab 上不显示数量，数量说明放在对应 Tab 内容区顶部。

### 按钮

线性按钮使用 `.helix-line-button`：

- 背景为半透明白色。
- hover 只保留白色扫光效果。
- hover 不改变文字颜色，不出现绿色圆点。
- 选中态按钮不触发扫光。

主按钮：

- 使用荧光绿背景。
- 文案使用深色文字。
- 只用于主要行动，例如“开始分析”。

### 标签与徽标

- 优先级标签使用 `.helix-priority-badge`，小尺寸定位符使用 `.helix-priority-badge-sm`。
- P0/P1/P2 只用于设计待办，不用于设计依据和 PRD 问题清单。
- 严重、致命等文档问题标签只出现在 PRD 问题清单，不能出现在设计待办。
- 标签应保持小尺寸，不要变成大胶囊按钮。

### 图标

- 当前项目使用 `@tabler/icons-react`。
- 不要混入新的图标库，也不要手写 raw SVG，除非是已有的上传成功云朵图标这类定制状态图形。
- 导航和按钮图标约 `20px`。
- 小提示或消息图标约 `16px`。
- 图标颜色跟随文字或组件主色，不单独使用新的强调色。

### 弹层

- 弹层必须居中显示，层级在所有页面元素之上。
- 背景遮罩使用 `bg-black/60` 和 `backdrop-blur-sm`。
- 弹层面板使用深色玻璃质感，不允许白底。
- API Key 等敏感信息不要出现在 URL、导出文件、日志或分析结果里。

## 5. 交互规范

### 上传

- 支持点击选择和拖拽上传。
- 上传区为虚线荧光绿边框。
- 未上传时显示上传图标；已上传时显示定制云朵成功图标。
- 上传区 hover / 鼠标移动允许有局部荧光绿光晕，但透明度要低。
- 扫描件 PDF 暂不支持的提示必须保留。

### 分析结果

- 默认打开 `设计待办`。
- 右侧三个 Tab 都必须独立滚动。
- 底部定位符跟随当前 Tab 切换，不同 Tab 不能共用同一组定位符。
- 顶部 `致命 / 严重 / 轻微 / 亮点` 指标可点击定位到对应内容首次出现位置。
- `设计重点` 应作为小块标题，正文信息层级低于标题。

### 导出

- `导出 HTML` 优先调用浏览器保存文件选择器，让用户选择保存路径。
- 保存成功后必须 toast 提示。
- 不支持保存选择器时，退回普通下载并 toast 提示。
- 用户取消保存不作为错误处理。

### 动效

- 背景 shader 使用荧光绿体系，透明度低，不能压过内容。
- 卡片 hover 只能轻微上移，不做夸张缩放。
- 线性按钮 hover 使用白色扫光。
- 遵守 `prefers-reduced-motion: reduce`，关闭 shader 和扫光等非必要动效。

## 6. 响应式规则

### 上传态

- 主体最大宽度：`min(1180px, calc(100% - 40px))`
- 桌面：左右两栏布局，比例约 `0.95fr / 1.05fr`
- 移动端：上下堆叠，导航操作收进菜单
- 上传面板和表单在小屏下保持单列

### 分析态

- 整体高度使用 `h-screen`，避免页面整体滚动失控。
- 顶部栏固定在布局上方，底部定位符在桌面端显示。
- 桌面：左侧文档预览 + 右侧分析结果双栏。
- 左侧文档预览宽度小于右侧分析区，当前建议为 `minmax(280px, 0.62fr)`。
- 右侧分析区最小宽度建议 `minmax(520px, 1fr)`。
- 小屏：两栏自动堆叠，保证内容可滚动和可阅读。
- Tab 按内容自适应宽度，超出时横向滚动。

## 7. 不应该破坏的设计约束

以下规则不要轻易改变：

- 不要把主题色替换为青色、蓝色、紫色或多色渐变；主色必须保持荧光绿 `#b7ff5a`。
- 不要把界面改成浅色主题。
- 不要移除背景网格、玻璃质感和低透明 shader，它们是当前视觉识别的一部分。
- 不要使用大面积纯绿色背景，荧光绿只做强调。
- 不要把分析结果重新拆回 `PRD 草稿 / 证据清单 / 导出` 这类旧 Tab。
- 不要在 `设计待办` 中展示“严重、致命”等文档问题标签。
- 不要在 `设计依据` 和 `PRD 问题清单` 中使用 P0/P1/P2。
- 不要恢复 PDF 缩略图，也不要增加 PDF 预览收起/展开功能。
- 不要把 Tab 按钮改成三等分等宽；必须按内容自适应。
- 不要让 Tab 切换出现白色背景闪烁。
- 不要让滚动条变成高亮白色，滚动条应保持暗黑低对比。
- 不要把顶部诊断分数做成大评分卡；评分应弱化，设计任务和问题内容才是主角。
- 不要引入其它图标库替换当前 Tabler 图标体系。
- 不要改变 API Key 的安全策略：只存在当前会话，不写入 URL、日志、导出文件或分析结果。
- 不要让卡片、按钮或文本在 1024px 宽度附近发生溢出或重叠。

## 实施检查清单

每次修改 UI 后至少检查：

- `npm run lint`
- `npm run build`
- `http://localhost:3000/` 能正常访问
- 上传态和分析态均无明显间距错乱
- 右侧 3 个 Tab 名称、顺序和职责未被破坏
- 导出文件中不包含 API Key
- 暗黑背景下文字对比度足够，按钮 hover 不改变既定风格
